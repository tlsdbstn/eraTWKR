
;-------------------------------------------
;触手の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_E
;触手が射精
SIF TFLAG:15 == 1
	PRINTFORML %CALLNAME:TARGET%에 관련되어 붙은 촉수가 끈적거리는 체액을 토해냈다…
;触手が大量射精
SIF TFLAG:15 == 2
	PRINTFORML %CALLNAME:TARGET%의 전신에 관련되어 붙은 촉수가 일제히 끈적거리는 체액을 토해냈다…
;テキストを読むためのウエイト
;表示があるときのみ
SIF TFLAG:15
	WAIT
;-------------------------------------------
;奴隷の焦らしのテキスト
;-------------------------------------------
@PALAM_MESSAGE_F
PRINTFORMW %CALLNAME:TARGET%하지만 당장 이 기초가 되었을 때를 가늠해,%CALLNAME:MASTER%(은)는 애무의 손을 끊어 버렸다
SIF !FLAG:70
	PRINTFORMW %CALLNAME:TARGET%(은)는 원망해 기분인 관심을 가지면서, 마지막 사람 밀기를 요구해 절에 몸을 구군요들 키라고 있다…

;-------------------------------------------
;奴隷の絶頂時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_B
#DIM OMORASHI

IF TCVAR:이뇨제 || TALENT:오줌싸개버릇
	OMORASHI = 1
ELSE
	OMORASHI = 0
ENDIF

LOCAL =  NOWEX:C절정 + NOWEX:V절정 + NOWEX:A절정 + NOWEX:B절정 + NOWEX:사정 + NOWEX:M절정
SIF !(LOCAL + NOWEX:방뇨)
	RETURN 0
;絶頂
IF FLAG:70 == 1
	PRINTFORM 시간을 제지당했다%CALLNAME:TARGET%하
	SIF NOWEX:분유
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%의%NIPPLE_DESCRIPTION(TARGET)%(으)로부터 모유를 늘어뜨려
	SIF LOCAL >= 5
		PRINT 음순으로부터 투명한 조수를 분출해
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%(으)로부터%SAMEN_DESCRIPTION(TARGET,1)%(을)를 분출해
	SIF NOWEX:분유 || LOCAL >= 5 || NOWEX:사정
		PRINT (이)면서,
	PRINTL 시간 정지로부터 해방되고 있다
	SIF NOWEX:사정 && TALENT:TARGET:2 > 1
		PRINTFORM %TINKO(TARGET)%
	SIF NOWEX:C절정 && TALENT:TARGET:2 == 1
		PRINT 클리토리스
	SIF NOWEX:C절정 && (NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정 || NOWEX:M절정)
		PRINT (와)과
	SIF NOWEX:V절정
		PRINT 질내
	SIF NOWEX:V절정 && (NOWEX:A절정 || NOWEX:B절정|| NOWEX:M절정)
		PRINT (와)과
	SIF NOWEX:A절정
		PRINT 어널
	SIF NOWEX:A절정 && (NOWEX:B절정|| NOWEX:M절정)
		PRINT (와)과
	SIF NOWEX:M절정
		PRINT 순
	SIF NOWEX:M절정 && NOWEX:B절정
		PRINT (와)과
	SIF NOWEX:B절정
		PRINTFORM %NIPPLE_DESCRIPTION(TARGET,0)%
	PRINTL 만을 진동시켜 절정에 이르렀다

	SIF NOWEX:V절정 && TEQUIP:50 == PLAYER
		PRINTFORML 절정 한 질내는 벌벌경련해, 찔러 넣을 수 있었다%TINKO(PLAYER)%에 관련되어 따라 온다…
	SIF NOWEX:A절정 && TEQUIP:51 == PLAYER
		PRINTFORML 절정 한 어널은, 찔러 넣을 수 있었다%TINKO(PLAYER)%(을)를 큐우큐우와 단단히 조여 온다…

	;絶頂とおもらし
	IF LOCAL >= 3 && TALENT:오줌싸개버릇
		PRINTFORML %CALLNAME:TARGET%의 신체는 쾌락의 너무 요도구를 느슨해지게 한 방뇨하고 있다…
	ENDIF
	
	;絶頂と噴乳
	IF LOCAL >= 3 && NOWEX:분유 && TEQUIP:유두빨기
		PRINTFORML %CALLNAME:MASTER%하%CALLNAME:TARGET%의%NIPPLE_DESCRIPTION(TARGET)%(을)를 빨아 올려 넘쳐 나오는 모유를 마시고 있다…
	ELSEIF NOWEX:분유 && TEQUIP:유두빨기
		PRINTFORML %CALLNAME:MASTER%하%CALLNAME:TARGET%의%NIPPLE_DESCRIPTION(TARGET)%(을)를 빨아 올려 배어 나오는 모유를 혀로 빨고 취하고 있다…
	ENDIF
	
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있다%CALLNAME:TARGET%하
	SIF LOCAL >= 5
		PRINT 음순으로부터 투명한 조수를 분출해
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%(으)로부터%SAMEN_DESCRIPTION(TARGET,1)%(을)를 분출해
	SIF NOWEX:분유 || LOCAL >= 5 || NOWEX:사정
		PRINT (이)면서,
	PRINTL 전신을 진동시켜 절정에 이르렀다

	SIF NOWEX:V절정 && TEQUIP:50 == PLAYER
		PRINTFORML 절정 한 질내는 벌벌경련해,%SAMEN_DESCRIPTION(PLAYER)%(을)를 짜 취하려고%TINKO(PLAYER)%에 관련되어 따라 온다…
	SIF NOWEX:A절정 && TEQUIP:51 == PLAYER
		PRINTFORML 절정 한 어널은, 찔러 넣을 수 있었다%TINKO(PLAYER)%(을)를 큐우큐우와 단단히 조여 온다…

	;絶頂と噴乳とおもらし
	CALL PrintMilkAndOmorashi(LOCAL, OMORASHI)
ELSE
	PRINTFORM %CALLNAME:TARGET%하\@ TCVAR:억지로? 目に涙を浮かべ# \@
	SIF LOCAL >= 5
		PRINT 음순으로부터 투명한 조수를 분출해
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%(으)로부터%SAMEN_DESCRIPTION(TARGET,1)%(을)를 분출해
	PRINTFORML \@ LOCAL < 5? 등을 크게 뒤로 젖히# \@면서, 전신을 진동시켜\@ SYNCED_ORGASM(TARGET)? %CALLNAME:MASTER%와 동시에# \@절정에 이르렀다

	IF NOWEX:사정
		IF TEQUIP:PLAYER:V섹스 == TARGET
			PRINTFORM %CALLNAME:PLAYER%의 질내에%CALLNAME%의%SAMEN_DESCRIPTION(TARGET,1)%하지만 쏟아졌다…
		ELSEIF TEQUIP:PLAYER:A섹스 == TARGET
			PRINTFORM %CALLNAME:PLAYER%의 어널에%CALLNAME%의%SAMEN_DESCRIPTION(TARGET,1)%하지만 쏟아졌다…
		ENDIF
	ENDIF

	;絶頂と噴乳とおもらし
	CALL PrintMilkAndOmorashi(LOCAL, OMORASHI)
ENDIF

;テキストを読むためのウエイト
;表示があるときのみ
IF LOCAL + NOWEX:방뇨 > 0
	WAIT
	SIF FLAG:대사색깔
		TRYCALLFORM M_KOJO_COLOR_K{NO:TARGET}
ENDIF

;-------------------------------------------
;調教者の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_A
VARSET LOCAL

LOCAL = NOWEX:MASTER:C절정 + NOWEX:MASTER:V절정 + NOWEX:MASTER:A절정 + NOWEX:MASTER:B절정 + NOWEX:MASTER:M절정
IF LOCAL >= 1
	PRINTFORM %CALLNAME:MASTER%하
	IF NOWEX:MASTER:오중절정 || NOWEX:MASTER:사중절정
		PRINTFORML 전신을 덮치는 강렬한 쾌감의 폭풍우에 소리를 높이면서 번민 미쳤다
	ELSE
		LOCAL:1 = 0
		IF NOWEX:MASTER:C절정
			IF TALENT:MASTER:성별 & 2
				PRINT 페니스
			ELSE
				PRINT 구리
			ENDIF
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:V절정
			SIF LOCAL:1
				PRINT (와)과
			PRINT 질
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:A절정
			SIF LOCAL:1
				PRINT (와)과
			PRINT 어널
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:B절정
			SIF LOCAL:1
				PRINT (와)과
			IF TALENT:MASTER:성별 & 1
				PRINT 흉
			ELSE
				PRINT 유두
			ENDIF
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:M절정
			SIF LOCAL:1
				PRINT (와)과
			PRINT 순
			LOCAL:1 = 1
		ENDIF
		IF LOCAL >= 5
			PRINTFORML (을)를 덮치는 강렬한 절정감에 머리가 새하얗게 되는 것을 느꼈다
		ELSE
			PRINTFORML 에의 쾌감으로 절정 해 몸을 진동시켰다
		ENDIF
	ENDIF
ENDIF

;TFLAG:1 射精箇所 (ビット0=コンドーム 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕
;20=手淫フェラ 21=パイズリフェラ22=シックスナイン 24=子宮口 25=疑似 26=授乳プレイ

;コンドーム
IF TCVAR:PLAYER:콘돔 == 1
	IF FLAG:70 == 1
		PRINTFORML %CALLNAME:PLAYER%의%TINKO(PLAYER)%하지만 토해냈다\@ NOWEX:MASTER:사정 >1 ? 大量の # \@%SAMEN_DESCRIPTION(PLAYER)%그리고 콘돔이 채워져 간다…
		IF GETBIT(TFLAG:1,1)
			SIF ABL:TARGET:질내사정중독
				PRINTFORML 기대하고 있던 것을 얻을 수  없었다%CALLNAME:(TARGET)%의 질내가, 불만에 쑤셨다…
		ELSEIF GETBIT(TFLAG:1,2)
			SIF ABL:TARGET:항내사정중독
				PRINTFORML 기대하고 있던 것을 얻을 수  없었다%CALLNAME:(TARGET)%의 아누스가, 불만에 쑤셨다…
		ENDIF
	ELSEIF CFLAG:MASTER:장난 == 2
		PRINTFORML %CALLNAME:PLAYER%의%TINKO(PLAYER)%하지만 토해냈다\@ NOWEX:MASTER:사정 >1 ? 大量の # \@%SAMEN_DESCRIPTION(PLAYER)%그리고 콘돔이 채워져 간다…
		PRINTFORM %TINKO(PLAYER)%(을)를 앞질러,
		SELECTCASE NOWEX:MASTER:사정
			CASE IS > 2
				PRINT 벌 있듯이
			CASE 2
				PRINT 첨단에 충분히
		ENDSELECT
		PRINTFORML 자식이 모인 콘돔을 이불의 옆으로 두었다…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%의%TINKO(PLAYER)%하지만 토해냈다\@ NOWEX:MASTER:사정 >1 ? 大量の # \@%SAMEN_DESCRIPTION(PLAYER)%그리고 콘돔이 채워져 간다…
		PRINTFORM %TINKO(PLAYER)%(을)를 앞질러,
		SELECTCASE NOWEX:MASTER:사정
			CASE IS > 2
				PRINT 벌 있듯이
			CASE 2
				PRINT 첨단에 충분히
		ENDSELECT
		PRINTFORML 자식이 모인 콘돔을%CALLNAME:(TARGET)%에 과시했다…
		;膣内射精
		IF GETBIT(TFLAG:1,1)
			IF (TALENT:TARGET:사모 || TALENT:TARGET:연모 || TALENT:TARGET:음란 || TALENT:TARGET:복종) && CFLAG:TARGET:생해금 != 2
				PRINTFORML %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%(을)를 기분 좋게하게 한 증거를 봐 만족기분이다…
			ELSEIF CFLAG:생해금 != 2 && !TALENT:TARGET:무지
				PRINTFORML %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%하지만 피임했던 것에 안심하고 있는 것 같다…
			ENDIF
			SIF ABL:질내사정중독
				PRINTFORML 기대하고 있던 것을 얻을 수  없었다%CALLNAME:(TARGET)%의 질내가, 불만에 쑤셨다…
		ELSEIF GETBIT(TFLAG:1,2)
			SIF (TALENT:TARGET:사모 || TALENT:TARGET:연모 || TALENT:TARGET:음란 || TALENT:TARGET:복종)
				PRINTFORML %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%(을)를 기분 좋게하게 한 증거를 봐 만족기분이다…
			SIF ABL:항내사정중독
				PRINTFORML 기대하고 있던 것을 얻을 수  없었다%CALLNAME:(TARGET)%의 아누스가, 불만에 쑤셨다…
		ENDIF
	ENDIF
	IF CFLAG:TARGET:자동고무
		IF TEQUIP:MASTER:콘돔
			PRINTFORML %CALLNAME:MASTER%(은)는 콘돔을 다시 붙였다
		ELSE
			PRINTFORML %CALLNAME:MASTER%(은)는 콘돔을 다시 붙이려고 했지만, 예비가 없었다
		ENDIF
	ENDIF
	WAIT
	RETURN
;個別処理
;子宮口責め
ELSEIF GETBIT(TFLAG:1,24)
	IF NOWEX:TARGET:V절정
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
			;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁마저 범해지는 열락에, 강렬한 절정을 맞이하고 있다
						PRINTFORML ?女(와)과 자궁구와 자궁벽에 말이야인 체해지며, 동시에 올라 채웠다%CALLNAME:PLAYER%하%SAMEN_DESCRIPTION(PLAYER)%(을)를 박았다
						PRINTFORML 어리고 작은 자궁의 구석구석까지%SAMEN_DESCRIPTION(PLAYER)%하지만 발라 담을 수 있을 때, 벌벌%CALLNAME:(TARGET)%의 질강이 경련한다
						PRINTFORMW %CALLNAME:(TARGET)%(은)는 시간을 제지당한 채로,\@ NOWEX:MASTER:사정 > 1 ? 下腹部が小さく膨らむほどの # \@%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 자궁에 쏟아졌다…
					ELSE
						PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 찔러지는 쾌감에 단번에 절정 했다
						PRINTFORML 간발 넣지 않고,%CALLNAME:PLAYER%하%TINKO(PLAYER)%의 첨단을 때굴때굴자궁구에 억눌러 정액을 발했다
						PRINTFORML %CALLNAME:(TARGET)%의 어리고 작은 자궁은 눈 깜짝할  순간에%SAMEN_DESCRIPTION(PLAYER,1)%그리고 채워지고
						PRINTFORMW 역류 했다%SAMEN_DESCRIPTION(PLAYER)%하지만 무모의 와레메로부터 넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 찔러지는 쾌감에 억지로 절정 당하고 있다
					PRINTFORML 와 비소를 강렬하게 단단히 조이면서, 와 달라붙도록(듯이) 접해 오는 자궁구에,%CALLNAME:MASTER%도 올라 채운다
					PRINTFORML 아이와 같이 어리다%CALLNAME:(TARGET)%의 좁은 질내를%TINKO(PLAYER)%그리고 채워, 귀두를 자궁에 꽉 눌러%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다
					PRINTFORMW 작은 자궁과 질내에서는 받아 들이지 못했다%SAMEN_DESCRIPTION(PLAYER)%하지만, 무모의 와레메로부터 넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML 질강을 강렬하게 단단히 조여 자궁구마저도 느슨하게 입을 열어, 귀두를 입에 물고 붐비면서%CALLNAME:(TARGET)%하지만 절정 하고 있다
					PRINTFORML 그 쾌감에 촉구받아,%CALLNAME:PLAYER%(은)는 질벽을%TINKO(PLAYER)%에 관련되어 붙게 한 자궁구에서 귀두를 입에 물고 붐비는 「기분 좋게 사정할 수 있는 구멍」에%TINKO(PLAYER)%(을)를 근원까지 찔러 넣고
					PRINTFORMW 시간을 제지당하고 「기분 좋게 사정할 수 있는 구멍」으로 되고 있다%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 마음껏 쏟았다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW 대량의%SAMEN_DESCRIPTION(PLAYER)%하지만 자궁에 발라 담겨져 있고 구
				ELSE
					PRINTFORML 자궁구를 찔러지는 감촉에 올라 채웠다%CALLNAME:(TARGET)%의 질내가, 강렬한 경련과 함께 단단히 조여 온다
					PRINTFORML %CALLNAME:MASTER%(을)를 거절하는것 같이 딱딱하게 닫았다%CALLNAME:(TARGET)%의 자궁구에,%TINKO(PLAYER)%(을)를 그리그리와 억누르면서 사정해,
					PRINTFORMW %SAMEN_DESCRIPTION(PLAYER,1)%그리고 자궁을 침략했다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW %CALLNAME:MASTER%(은)는 정복감에 취해 심취하면서, 대량의%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
				ENDIF
			;---------------------------------
			;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 시간을 제지당하면서도, 절정에 경련한다%CALLNAME:(TARGET)%의 질 깊고%TINKO(PLAYER)%(을)를 강하게 찔러 붐비었다%CALLNAME:MASTER%(은)는,
				PRINTFORMW 달라붙어 오는 자궁구에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ELSE
				PRINTFORML %CALLNAME:(TARGET)%(은)는, 질내를 벌벌조금씩 경련시켜, 절정 하고 있다
				PRINTFORML %CALLNAME:PLAYER%(은)는 자궁구에 빈 얼마 안되는구멍에 령구를 밀착시켜
				PRINTFORMW 직접 자궁구에%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:장난 == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁마저 범해지는 열락에, 강렬한 절정을 맞이하고 있다
						PRINTFORML ?女(와)과 자궁구와 자궁벽에 말이야인 체해지며, 동시에 올라 채웠다%CALLNAME:PLAYER%하%SAMEN_DESCRIPTION(PLAYER)%(을)를 박았다
						PRINTFORML 어리고 작은 자궁의 구석구석까지%SAMEN_DESCRIPTION(PLAYER)%하지만 발라 담을 수 있을 때, 벌벌%CALLNAME:(TARGET)%의 질강이 경련한다
						PRINTFORMW %CALLNAME:(TARGET)%(은)는 잔 채로,\@ NOWEX:MASTER:사정 > 1 ? 下腹部が小さく膨らむほどの # \@%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 자궁에 쏟아졌다…
					ELSE
						PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 찔러지는 쾌감에 단번에 절정 했다
						PRINTFORML 간발 넣지 않고,%CALLNAME:PLAYER%하%TINKO(PLAYER)%의 첨단을 때굴때굴자궁구에 억눌러 정액을 발했다
						PRINTFORML %CALLNAME:(TARGET)%의 어리고 작은 자궁은 눈 깜짝할  순간에%SAMEN_DESCRIPTION(PLAYER,1)%그리고 채워지고
						PRINTFORMW 역류 했다%SAMEN_DESCRIPTION(PLAYER)%하지만 무모의 와레메로부터 넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 찔러지는 쾌감에 억지로 절정 당하고 있다
					PRINTFORML 와 비소를 강렬하게 단단히 조이면서, 와 달라붙도록(듯이) 접해 오는 자궁구에,%CALLNAME:MASTER%도 올라 채운다
					PRINTFORML 아이와 같이 어리다%CALLNAME:(TARGET)%의 좁은 질내를%TINKO(PLAYER)%그리고 채워, 귀두를 자궁에 꽉 눌러%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다
					PRINTFORMW 작은 자궁과 질내에서는 받아 들이지 못했다%SAMEN_DESCRIPTION(PLAYER)%하지만, 무모의 와레메로부터 넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML 질강을 강렬하게 단단히 조여 자궁구마저도 느슨하게 입을 열어, 귀두를 입에 물고 붐비면서%CALLNAME:(TARGET)%하지만 절정 하고 있다
					PRINTFORML 그 쾌감에 촉구받아,%CALLNAME:PLAYER%(은)는 질벽을%TINKO(PLAYER)%에 관련되어 붙게 한 자궁구에서 귀두를 입에 물고 붐비는 「기분 좋게 사정할 수 있는 구멍」에%TINKO(PLAYER)%(을)를 근원까지 찔러 넣고
					PRINTFORMW 잔 채로 범해지고 「기분 좋게 사정할 수 있는 구멍」으로 되고 있다%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 마음껏 쏟았다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW 대량의%SAMEN_DESCRIPTION(PLAYER)%하지만 자궁에 발라 담겨져 있고 구
				ELSE
					PRINTFORML 자궁구를 찔러지는 감촉에 올라 채웠다%CALLNAME:(TARGET)%의 질내가, 강렬한 경련과 함께 단단히 조여 온다
					PRINTFORML %CALLNAME:MASTER%(을)를 거절하는것 같이 딱딱하게 닫았다%CALLNAME:(TARGET)%의 자궁구에%TINKO(PLAYER)%(을)를 그리그리와 억누르면서 사정해,
					PRINTFORMW %SAMEN_DESCRIPTION(PLAYER,1)%그리고 자궁을 침략했다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW %CALLNAME:MASTER%(은)는 정복감에 취해 심취하면서, 대량의%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
				ENDIF
			;---------------------------------
			;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 자면서, 절정에 경련한다%CALLNAME:(TARGET)%의 질 깊고
				PRINTFORML %TINKO(PLAYER)%(을)를 강하게 찔러 붐비었다%CALLNAME:MASTER%(은)는,
				PRINTFORMW 달라붙어 오는 자궁구에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ELSE
				PRINTFORML %CALLNAME:(TARGET)%(은)는, 질내를 벌벌조금씩 경련시켜, 절정 하고 있다
				PRINTFORML %CALLNAME:PLAYER%(은)는 자궁구에 빈 얼마 안되는구멍에 령구를 밀착시켜
				PRINTFORMW 직접 자궁구에%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:자궁구개발경험 >= 300
				IF TEQUIP:TARGET:체위2 == 1
					PRINTFORML 마지막에 마음껏 허리를 쑥 내밀어,%CALLNAME:(TARGET)%의 태내 깊고로 굄에 모인 욕망따돌릴 수 있었다…
					PRINTFORML 자궁안에 뜨겁다%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟아진다%CALLNAME:(TARGET)%하지만, 미육을 진동시키면서 절정 한다
					PRINTFORML 벌벌허벅지를 경련시켜, 필사적으로%CALLNAME:PLAYER%에 매달린다%CALLNAME:(TARGET)%
					;PRINTFORML %CALLNAME:(TARGET)%は両脚をつま先までぴんと張り詰めさせ、雌肉をわななかせて射精を受け止めている…
					PRINTFORMW %CALLNAME:PLAYER%(은)는 단단히%CALLNAME:(TARGET)%(을)를 꼭 껴안아 밀착 상태를 유지한 채로%SAMEN_DESCRIPTION(PLAYER)%(을)를 계속 쏟았다…
				ELSE
					PRINTFORML 입길로부터 군침을 늘어뜨리면서 단?울음을 주어 올라 채운다%CALLNAME:(TARGET)%
					PRINTFORML 강렬한 억압과 복잡한 경련에 유발되어%CALLNAME:PLAYER%(은)는 솟구치는 사정감에 항무사히,%TINKO(PLAYER)%그리고 자궁구를 관철한 채로
					PRINTFORMW 직접%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다
					SIF TALENT:TARGET:동물귀 && TEQUIP:TARGET:체위2 == 2
						PRINTFORML 짐승의 자세에서의 교배가,%CALLNAME:(TARGET)%에 지상의 쾌락을 가져오고 있다……
				ENDIF
			ELSE
				PRINTFORML 벌벌전신을 진동시키면서,%CALLNAME:(TARGET)%(은)는 절정에 몸을 맡기고 있다
				PRINTFORML %CALLNAME:PLAYER%(은)는 경련한다%CALLNAME:(TARGET)%의 질오에 딱%TINKO(PLAYER)%(을)를 고정해
				PRINTFORMW 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ENDIF
			IF TCVAR:TARGET:억지로
				PRINTFORML %CALLNAME:(TARGET)%(은)는 태내 깊게 퍼지는 점액의 감촉에 녹은 것 같은 표정을 띄운 뒤, 제 정신을 차리고라는 률 있다…
			ELSE
				PRINTFORML …
				PRINTFORML 
				IF CFLAG:TARGET:생해금 < 2 && 妊娠可否(TARGET) && !TALENT:TARGET:무지
					PRINTFORMW 쾌락에 흐린 머리로,%CALLNAME:(TARGET)%(은)는 멍하니 임신의 가능성에 생각을 둘러싸게 했다……
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%(은)는 자궁을 채우는 뜨거운 감촉에, 강렬한 절정감과 여자의 행복을 느끼고 있다…
				ENDIF
				PRINTFORMW …
			ENDIF
		ENDIF
	ELSE
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
				;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 자궁의 안쪽까지 페니스를 찔러 넣어 사정했다
						PRINTFORML 토해내졌다%SAMEN_DESCRIPTION(PLAYER)%(은)는 자궁구를 페니스로 찰 수 있는 도망갈 장소가 없고, 모두%CALLNAME:(TARGET)%의 어리고 작은 자궁에 쏟아져 간다
						PRINTFORMW %CALLNAME:(TARGET)%(은)는 시간을 제지당한 채로,\@ NOWEX:MASTER:사정 > 1 ? 下腹部が小さく膨らむほどの # \@%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 자궁에 쏟아졌다…
					ELSE
						PRINTFORML %CALLNAME:PLAYER%하%TINKO(PLAYER)%의 첨단을%CALLNAME:(TARGET)%의 자궁구붐비게 한 채로 사정했다
						PRINTFORML %CALLNAME:(TARGET)%의 어리고 작은 자궁은 눈 깜짝할  순간에%SAMEN_DESCRIPTION(PLAYER,1)%그리고 채워지고
						PRINTFORMW 역류 했다%SAMEN_DESCRIPTION(PLAYER)%하지만 무모의 와레메로부터 넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML 아이와 같이 어리다%CALLNAME:(TARGET)%의 좁은 질내를%TINKO(PLAYER)%그리고 채워, 귀두를 자궁에 꽉 눌러%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다
					PRINTFORMW 작은 자궁과 질내에서는 받아 들이지 못했다%SAMEN_DESCRIPTION(PLAYER)%하지만, 무모의 와레메로부터 넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML %CALLNAME:PLAYER%(은)는 질벽을%TINKO(PLAYER)%에 관련되어 붙게 한 자궁구에서 귀두를 입에 물고 붐비는 「기분 좋게 사정할 수 있는 구멍」에%TINKO(PLAYER)%(을)를 근원까지 찔러 넣고
					PRINTFORMW 시간을 제지당하고 「기분 좋게 사정할 수 있는 구멍」으로 되고 있다%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 마음껏 쏟았다…
				ELSE
					PRINTFORML %CALLNAME:MASTER%(을)를 거절하는것 같이 딱딱하게 닫았다%CALLNAME:(TARGET)%의 자궁구에%TINKO(PLAYER)%(을)를 그리그리와 억누르면서 사정해,
					PRINTFORMW %SAMEN_DESCRIPTION(PLAYER,1)%그리고\@ CFLAG:TARGET:생리주기 == 7 ? 危険日の # \@자궁을 침략했다…
				ENDIF
				SIF NOWEX:MASTER:사정 > 1
					PRINTFORMW %CALLNAME:MASTER%(은)는 정복감에 취해 심취하면서, 대량의%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			;---------------------------------
				;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 시간을 제지당했다%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 깊게 찔러 넣고
				PRINTFORMW 달라붙어 오는 자궁구에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ELSE
				PRINTFORML %CALLNAME:PLAYER%(은)는 자궁구에 빈 얼마 안되는구멍에 령구를 밀착시켜
				PRINTFORMW 직접 자궁구에%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:장난 == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 자궁의 안쪽까지 페니스를 찔러 넣어 사정했다
						PRINTFORML 토해내졌다%SAMEN_DESCRIPTION(PLAYER)%(은)는 자궁구를 페니스로 찰 수 있는 도망갈 장소가 없고, 모두%CALLNAME:(TARGET)%의 어리고 작은 자궁에 쏟아져 간다
						PRINTFORMW %CALLNAME:(TARGET)%(은)는 잔 채로,\@ NOWEX:MASTER:사정 > 1 ? 下腹部が小さく膨らむほどの # \@%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 자궁에 쏟아졌다…
					ELSE
						PRINTFORML %CALLNAME:PLAYER%하%TINKO(PLAYER)%의 첨단을%CALLNAME:(TARGET)%의 자궁구붐비게 한 채로 사정했다
						PRINTFORML %CALLNAME:(TARGET)%의 어리고 작은 자궁은 눈 깜짝할  순간에%SAMEN_DESCRIPTION(PLAYER,1)%그리고 채워지고
						PRINTFORMW 역류 했다%SAMEN_DESCRIPTION(PLAYER)%하지만 무모의 와레메로부터 넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML 아이와 같이 어리다%CALLNAME:(TARGET)%의 좁은 질내를%TINKO(PLAYER)%그리고 채워, 귀두를 자궁에 꽉 눌러%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다
					PRINTFORMW 작은 자궁과 질내에서는 받아 들이지 못했다%SAMEN_DESCRIPTION(PLAYER)%하지만, 무모의 와레메로부터 넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML %CALLNAME:PLAYER%(은)는 질벽을%TINKO(PLAYER)%에 관련되어 붙게 한 자궁구에서 귀두를 입에 물고 붐비는 「기분 좋게 사정할 수 있는 구멍」에%TINKO(PLAYER)%(을)를 근원까지 찔러 넣고
					PRINTFORMW 잔 채로 범해지고 「기분 좋게 사정할 수 있는 구멍」으로 되고 있다%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 마음껏 쏟았다…
				ELSE
					PRINTFORML %CALLNAME:MASTER%(을)를 거절하는것 같이 딱딱하게 닫았다%CALLNAME:(TARGET)%의 자궁구에%TINKO(PLAYER)%(을)를 그리그리와 억누르면서 사정해,
					PRINTFORMW %SAMEN_DESCRIPTION(PLAYER,1)%그리고 자궁을 침략했다…
				ENDIF
				SIF NOWEX:MASTER:사정 > 1
					PRINTFORMW %CALLNAME:MASTER%(은)는 정복감에 취해 심취하면서, 대량의%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			;---------------------------------
				;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 자고 있다%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 깊게 찔러 넣고
				PRINTFORMW 달라붙어 오는 자궁구에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ELSE
				PRINTFORML %CALLNAME:PLAYER%(은)는 자궁구에 빈 얼마 안되는구멍에 령구를 밀착시켜
				PRINTFORMW 직접 자궁구에%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟았다…
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:자궁구개발경험 >= 300
				IF TEQUIP:TARGET:체위2 == 1
					PRINTFORML 마지막에 마음껏 허리를 쑥 내밀어,%CALLNAME:(TARGET)%의 태내 깊고로 굄에 모인 욕망따돌릴 수 있었다…
					PRINTFORML %CALLNAME:(TARGET)%(은)는 양각을 발끝까지 즉각 긴장되게 해 자육을 함정 울려 사정을 받아 들이고 있다…
					PRINTFORML %CALLNAME:PLAYER%(은)는 단단히%CALLNAME:(TARGET)%(을)를 꼭 껴안아 밀착 상태를 유지한 채로%SAMEN_DESCRIPTION(PLAYER)%(을)를 계속 쏟았다…
				ELSE
					PRINTFORML %CALLNAME:PLAYER%(은)는 솟구치는 사정감에 항무사히,%TINKO(PLAYER)%그리고 자궁구를 관철한 채로
					PRINTFORML 직접%CALLNAME:(TARGET)%의 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다
				ENDIF
			ELSE
				PRINTFORML %CALLNAME:PLAYER%하\@ TCVAR:TARGET:억지로? 嫌がる# \@%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 고정해
				PRINTFORMW 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ENDIF
			IF TCVAR:TARGET:억지로
				PRINTFORML %CALLNAME:(TARGET)%(은)는 태내 깊게 퍼지는 점액의 감촉에 률 있다…
			ELSEIF TALENT:TARGET:연모
				PRINTFORML …
				PRINTFORML 
				PRINTFORMW %CALLNAME:(TARGET)%(은)는 자궁을 채우는 뜨거운 감촉에 여자의 행복을 느끼고 있다…
			ELSEIF CFLAG:TARGET:생해금 < 2 && 妊娠可否(TARGET) && !TALENT:TARGET:무지
				PRINTFORMW %CALLNAME:(TARGET)%의 뇌리에 임신의 가능성이 지나갔다……
			ELSE
				PRINTFORMW …
			ENDIF
		ENDIF
	ENDIF
;シックスナイン
ELSEIF GETBIT(TFLAG:1,22)
	IF NOWEX:TARGET:C절정 || NOWEX:TARGET:M절정
		IF FLAG:70 == 1
			IF NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML \@ GETBIT(TALENT:TARGET:성별,1) ? %TINKO((TARGET))% # 클리토리스 \@と口腔を震わせ、%CALLNAME:(TARGET)%하지만 올라 채우고 있다
			ELSEIF NOWEX:TARGET:C절정 && !NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의\@ GETBIT(TALENT:TARGET:성별,1) ? %TINKO((TARGET))% # 클리토리스 \@が絶頂に震えている
			ELSEIF !NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 입안이 절정에 떨고 있다
			ENDIF
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORMW 시간을 제지당했다%CALLNAME:(TARGET)%하지만%CALLNAME:PLAYER%의 입안에 사정했다고 동시에%CALLNAME:PLAYER%도%CALLNAME:(TARGET)%의 입안에 사정했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML 시간을 제지당했다%CALLNAME:(TARGET)%하%TINKO((TARGET))%만을 벌벌뛰게 해%CALLNAME:PLAYER%의 입안에 사정했다
					PRINTFORMW %CALLNAME:PLAYER%하%SAMEN_DESCRIPTION((TARGET),1)%(을)를 입으로 받으면서%CALLNAME:(TARGET)%의 입에%TINKO(PLAYER)%(을)를 찔러 넣었다…
				ELSE
					PRINTFORMW 시간을 제지당했다%CALLNAME:(TARGET)%하%TINKO((TARGET))%만을 벌벌뛰게 해%CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:(TARGET)%의 남성기를 빨면서
				ELSE
					PRINTFORM %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:(TARGET)%의 비소에 혀를은 조생이면서
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에서 사정했다…
				PRINTFORMW %CALLNAME:PLAYER%(은)는 히크 붙는 비렬달라붙어서 놓지 않으면서,%CALLNAME:(TARGET)%의 후오까지%TINKO(PLAYER)%(을)를 찔러 넣고%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 흘려 넣어 간다…
			ENDIF
		ELSE
			IF NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML \@ GETBIT(TALENT:TARGET:성별,1) ? %TINKO((TARGET))% # 클리토리스 \@と口腔を震わせ、%CALLNAME:(TARGET)%하지만 올라 채우고 있다
			ELSEIF NOWEX:TARGET:C절정 && !NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의\@ GETBIT(TALENT:TARGET:성별,1) ? %TINKO((TARGET))% # 클리토리스 \@が絶頂に震えている
			ELSEIF !NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 입안이 절정에 떨고 있다
			ENDIF
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORML %CALLNAME:(TARGET)%(와)과%CALLNAME:PLAYER%(은)는 동시에 서로의 입안에 사정했다…
				PRINTFORMW 두 명은 서로의 페니스를 근원까지 질네쾌락을 서로 확인했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%보다 먼저 사정해 버렸다…
					PRINTFORMW %CALLNAME:PLAYER%하%SAMEN_DESCRIPTION((TARGET))%(을)를 입으로 받으면서%CALLNAME:(TARGET)%의 입에%TINKO(PLAYER)%(을)를 찔러 넣었다
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 남성기를 빨면서
				ELSE
					PRINTFORM %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 비소에 혀를은 조생이면서
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에서 사정했다…
				PRINTFORMW %CALLNAME:(TARGET)%(은)는 하반신에의 자극과 구강의 열락에 벌벌반응하면서 분출한다%SAMEN_DESCRIPTION(PLAYER)%(을)를 훌쩍거리고 있다…
			ENDIF
		ENDIF
	ELSE
		IF FLAG:70 == 1
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORMW 시간을 제지당했다%CALLNAME:(TARGET)%하지만%CALLNAME:PLAYER%의 입안에 사정했다고 동시에%CALLNAME:PLAYER%도%CALLNAME:(TARGET)%의 입안에 사정했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER & 2
					PRINTFORML 시간을 제지당했다%CALLNAME:(TARGET)%하%TINKO((TARGET))%만을 벌벌뛰게 해%CALLNAME:PLAYER%의 입안에 사정했다
					PRINTFORMW %CALLNAME:PLAYER%하%SAMEN_DESCRIPTION((TARGET),1)%(을)를 입으로 받으면서%CALLNAME:(TARGET)%의 입에%TINKO(PLAYER)%(을)를 찔러 넣었다…
				ELSE
					PRINTFORMW 시간을 제지당했다%CALLNAME:(TARGET)%하%TINKO((TARGET))%만을 벌벌뛰게 해%CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:(TARGET)%의 남성기를 빨면서
				ELSE
					PRINTFORM %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:(TARGET)%의 비소에 혀를은 조생이면서
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에서 사정했다…
				PRINTFORMW %CALLNAME:PLAYER%(은)는 히크 붙는 비렬달라붙어서 놓지 않으면서,%CALLNAME:(TARGET)%의 후오까지%TINKO(PLAYER)%(을)를 찔러 넣고%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 흘려 넣어 간다…
			ENDIF
		ELSE
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORML %CALLNAME:(TARGET)%(와)과%CALLNAME:PLAYER%(은)는 동시에 서로의 입안에 사정했다…
				PRINTFORMW 두 명은 서로의 페니스를 근원까지 질네쾌락을 서로 확인했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%보다 먼저 사정해 버렸다…
					PRINTFORMW %CALLNAME:PLAYER%하%SAMEN_DESCRIPTION((TARGET))%(을)를 입으로 받으면서%CALLNAME:(TARGET)%의 입에%TINKO(PLAYER)%(을)를 찔러 넣었다
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 남성기를 빨면서
				ELSE
					PRINTFORM %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 비소에 혀를은 조생이면서
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에서 사정했다…
				PRINTFORMW %CALLNAME:(TARGET)%(은)는 하반신에의 자극에 벌벌반응하면서 분출한다%SAMEN_DESCRIPTION(PLAYER)%(을)를 훌쩍거리고 있다…
			ENDIF
		ENDIF
	ENDIF
;授乳プレイ
ELSEIF GETBIT(TFLAG:1,26)
	PRINTFORML 사정이 시작되면(자)%CALLNAME:(TARGET)%하%NIPPLE_DESCRIPTION(TARGET)%(을)를%CALLNAME:(MASTER)%의 입에 꽉 누르면서, 한층 격렬하고%TINKO(PLAYER)%(을)를 훑어 내 세우고
	
	IF ABL:TARGET:정액중독 >= 5 && ABL:TARGET:기교 > 3
		PRINTFORML 황홀의 표정으로\@ NOWEX:PLAYER:사정 > 1 ? 大量に噴き上がる # 吐き出される \@%CALLNAME:PLAYER%의%SAMEN_DESCRIPTION(PLAYER)%(을)를 받고 있다…
	ELSEIF NOWEX:PLAYER:사정 == 1
		PRINTFORML \@ NOWEX:PLAYER:사정 > 1 ? 大量に噴き上がる # 吐き出される \@%CALLNAME:PLAYER%의%SAMEN_DESCRIPTION(PLAYER)%(을)를 받고 있다…
	ELSE
		PRINTFORML %CALLNAME:(TARGET)%의 얼굴과 손을,\@ NOWEX:PLAYER:사정 > 1 ? 大量に噴き上がる # 吐き出される \@%CALLNAME:PLAYER%의%SAMEN_DESCRIPTION(PLAYER)%투성이가 되게 했다…
	ENDIF 
	PRINTFORM %CALLNAME:(TARGET)%하
	SIF NOWEX:TARGET:B절정
		PRINTFORM 동시에 유두로부터도 늘어뜨려지는 절정의 감각에 몸부림 하면서
	;噴乳は地の文に任せた
	PRINTFORMW 그 후도 천천히%CALLNAME:MASTER%의%TINKO(PLAYER)%의 맥동이 멈출 때까지 애무를 계속했다…
;パイズリフェラ
ELSEIF GETBIT(TFLAG:1,21)
	IF NOWEX:TARGET:B절정 || NOWEX:TARGET:M절정
		IF FLAG:70 == 1
			PRINTFORML %CALLNAME:(TARGET)%의%OPPAI_DESCRIPTION((TARGET))%그리고%TINKO(PLAYER)%(을)를 훑어 내면서%CALLNAME:(TARGET)%의 얼굴에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 뿌렸다…
			IF NOWEX:TARGET:B절정 && !NOWEX:TARGET:M절정
				PRINTFORML %TINKO(PLAYER)%\@ TALENT:TARGET:가슴사이즈 >= 0 ? を包み込む # をこすりつけている \@%OPPAI_DESCRIPTION((TARGET))%하지만
			ELSEIF !NOWEX:TARGET:B절정 && NOWEX:TARGET:M절정
				PRINTFORML %TINKO(PLAYER)%(을)를 입에 물고 입 뛰어날 수 있고 입안이
			ELSE
				PRINTFORML %TINKO(PLAYER)%\@ TALENT:TARGET:가슴사이즈 >= 0 ? を包み込む # をこすりつけている \@%OPPAI_DESCRIPTION((TARGET))%라고
				PRINTFORML 귀두를 입에 물고 입 뛰어날 수 있고 입안이
			ENDIF
			PRINTFORML , 절정에 떨고 있다
		ELSE
			PRINTFORML %CALLNAME:(TARGET)%의%OPPAI_DESCRIPTION((TARGET))%에 끼우게 했다%TINKO(PLAYER)%의 첨단으로부터%SAMEN_DESCRIPTION(PLAYER,1)%하지만 분출했다…
			IF NOWEX:TARGET:B절정 && !NOWEX:TARGET:M절정
				PRINTFORML %TINKO(PLAYER)%\@ TALENT:TARGET:가슴사이즈 >= 0 ? を包み込む # をこすりつけている \@%OPPAI_DESCRIPTION((TARGET))%그리고
			ELSEIF !NOWEX:TARGET:B절정 && NOWEX:TARGET:M절정
				PRINTFORML %TINKO(PLAYER)%(을)를 입에 물고 붐비고 있는 입안으로부터의 쾌감에
			ELSE
				PRINTFORML %TINKO(PLAYER)%\@ TALENT:TARGET:가슴사이즈 >= 0 ? を包み込む # をこすりつけている \@%OPPAI_DESCRIPTION((TARGET))%라고
				PRINTFORML 귀두를 입에 물고 붐비고 있는 입안에서
			ENDIF
			PRINTFORML 절정 했다%CALLNAME:(TARGET)%하지만 넑을 잃고 있다
		ENDIF
	ELSE
		IF FLAG:70 == 1
			PRINTFORMW %CALLNAME:(TARGET)%의%OPPAI_DESCRIPTION((TARGET))%그리고%TINKO(PLAYER)%(을)를 훑어 내면서%CALLNAME:(TARGET)%의 얼굴에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 뿌렸다…
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)%의%OPPAI_DESCRIPTION((TARGET))%에 끼우게 했다%TINKO(PLAYER)%의 첨단으로부터%SAMEN_DESCRIPTION(PLAYER,1)%하지만 분출했다…
		ENDIF
	ENDIF
;手淫フェラ
ELSEIF GETBIT(TFLAG:1,20)
	IF NOWEX:TARGET:M절정
		PRINTFORML 입안을 묻을 수 있는 쾌감에 절정 했다%CALLNAME:(TARGET)%의 목에 노려,%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다…
	ELSE
		PRINTFORML %CALLNAME:(TARGET)%의 목에 노려%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발했다…
	ENDIF
	;精飲
	IF EXP_UP(15,TARGET) == NOWEX:PLAYER:사정 * 3
		PRINTFORML %CALLNAME:(TARGET)%(은)는 사정의 기세등무늬도 제대로 귀두를 질
		PRINTFORMW 짜기 시작하는 것 같은 손놀림으로 줄기의 부분을 훑어 내고 있다…
		;RETURN 1
	ELSE
		IF TCVAR:TARGET:정음경험 == 0
			PRINTFORML %CALLNAME:(TARGET)%(은)는 놀라%TINKO(PLAYER)%(으)로부터 입을 떼어 놓아 버렸다
			PRINTFORMW 얼굴을%SAMEN_DESCRIPTION(PLAYER)%그리고 더럽히면서 처음의 맛에 조금 당황하고 있다…
		ELSE
			PRINTFORML %CALLNAME:(TARGET)%(은)는 쑥 내민 혀로 요도구를 자극하면서
			PRINTFORMW 짜기 시작하는 것 같은 손놀림으로 줄기의 부분을 훑어 내고 있다……
		ENDIF
		;RETURN 2
	ENDIF
;部位別処理
;膣内
ELSEIF TCVAR:PLAYER:12 == 1
	IF TFLAG:50 == 13
		PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TFLAG:152)%의 질내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 발하면(자) 일단%TINKO(PLAYER)%(을)를 뽑고 나서
		PRINTFORML 사정이 멈추지 않는 동안에%CALLNAME:(TARGET)%에 삽입해, 마지막 한 방울까지 쏟았다…
		SIF NOWEX:TARGET:V절정
			PRINTFORML 질강을 꾸물거려지는 쾌감과 정액을 쏟아지는 열락에,%CALLNAME:(TARGET)%(은)는 전신을 진동시키고 올라 채우고 있다
	;時間停止or睡眠姦
	ELSEIF FLAG:70 || CFLAG:MASTER:장난 == 2
		IF TFLAG:50 == 7
			IF (FLAG:70 && EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50) || (CFLAG:MASTER:장난 == 2 && EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50)
				IF EXP:TARGET:자궁구개발경험 >= 50
					PRINTFORML %CALLNAME:PLAYER%(은)는 솟구치는 사정감에 항무사히,%TINKO(PLAYER)%(을)를 깊게 찔러 넣었다…
					PRINTFORMW \@ FLAG:70 ? 時間を止められ何も認識出来ない # 眠っている \@%CALLNAME:(TARGET)%의 자궁구를 관철한 채로, 직접 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
				ELSE
					PRINTFORML %CALLNAME:PLAYER%하\@ FLAG:70 ? 時間を止められ抵抗できない # 眠っている \@%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 고정해
					PRINTFORMW 일절 주저 하는 일 없이 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
				ENDIF
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 깊게 찔러 넣고
				PRINTFORMW 달라붙어 오는 자궁구에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ELSE
				PRINTFORML \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 고정해
				PRINTFORMW 자궁내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			ENDIF
			SIF NOWEX:TARGET:V절정
				PRINTFORML \@ FLAG:70 ? 時間が止まった # 本人が眠る \@안, 그런데도 절정에 벌벌떨리는 질강은, 한층 더%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 바라고 있다
		ELSE
			IF (FLAG:70 && EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50) || (CFLAG:MASTER:장난 == 2 && EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50)
				IF TALENT:TARGET:체형 == -2
					SIF NOWEX:TARGET:V절정
						PRINTFORML %CALLNAME:(TARGET)%의 몸은 좁은 질강과 자궁구를 꾸물거려지는 감촉에 올라 채웠다
					IF EXP:TARGET:자궁구개발경험 >= 50
						PRINTFORML %CALLNAME:MASTER%(은)는 작은%CALLNAME:(TARGET)%의 질에 무리하게%TINKO(PLAYER)%(을)를 근원까지 밀어넣어,%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다
						PRINTFORML 토해내졌다%SAMEN_DESCRIPTION(PLAYER)%(은)는 눈 깜짝할  순간에 작은 질내를 채워, 어리게 해 개발되어 버린 자궁구에 흘러들었다
						PRINTFORMW %CALLNAME:MASTER%(은)는 외형 그대로의 어린 성기의 강렬한 억압을 즐기면서, 마지막 한 방울까지 쏟았다…
					ELSE
						PRINTFORML %CALLNAME:MASTER%(은)는 작은%CALLNAME:(TARGET)%의 질에 무리하게%TINKO(PLAYER)%(을)를 근원까지 밀어넣어,%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다
						PRINTFORMW 토해내졌다%SAMEN_DESCRIPTION(PLAYER)%(은)는 눈 깜짝할  순간에 작은 질내를 채워, 기치기치에 넓힐 수 있었던 결합부로부터 넘치고 있다… 
					ENDIF
				ELSEIF EXP:TARGET:자궁구개발경험 >= 50
					SIF NOWEX:TARGET:V절정
						PRINTFORML 절정 했다%CALLNAME:(TARGET)%의 질강이, 벌벌경련 품질와 단단히 조여 정액을 재촉 해 온다
					PRINTFORML 일절 주저 하는 일 없이%CALLNAME:(TARGET)%의 질오에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 마구 따돌렸다
					PRINTFORML %SAMEN_DESCRIPTION(PLAYER)%변소로 된 질혈은 토해내진다%SAMEN_DESCRIPTION(PLAYER)%(을)를 열심히 빨아 올려 벌벌뛴다%TINKO(PLAYER)%(을)를 상냥하게 단단히 조여 온다
					PRINTFORMW %CALLNAME:MASTER%(은)는 「기분 좋게 사정할 수 있는 구멍」을 사용해 기분 좋고 성욕 처리를 했다…
				ELSE
					SIF NOWEX:TARGET:V절정
						PRINTFORML 올라 채웠다%CALLNAME:(TARGET)%의 질내가,%TINKO(PLAYER)%(을)를 아주 같게 불쾌하고 준구
					IF EX:TARGET:처녀상실체크
						PRINTFORML %CALLNAME:MASTER%(은)는 가차 없이질내에서 사정해, 누구에게도 더럽혀진 적이 없었던 처녀질에 마음껏 정액으로 마킹 했다…
					ELSE
						PRINTFORML %CALLNAME:MASTER%(은)는 주저 없고 고기 오나호로 하고 있다%CALLNAME:(TARGET)%의 질내에 사정했다
					ENDIF
					PRINTFORML \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%(은)는, 질혈을 오나호 대신에 사용되고 질내 사정되고 있다…
				ENDIF
			ELSEIF TALENT:TARGET:체형 == -2
				SIF NOWEX:TARGET:V절정
					PRINTFORML %CALLNAME:(TARGET)%의 몸은, 좁은 질강과 자궁구를 꾸물거려지는 감촉에 올라 채웠다
				PRINTFORML \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%의 질내에 사정했다
				PRINTFORMW 아이와 같이 작은 질내는 눈 깜짝할  순간에 채워져 결합부로부터%SAMEN_DESCRIPTION(PLAYER,1)%하지만 넘쳐 나오고 있다…
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				SIF NOWEX:TARGET:V절정
					PRINTFORML %CALLNAME:(TARGET)%의 질강은, 자궁구를 얻어맞는 쾌감에 절정 했다
				PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 질오에 사정했다
				PRINTFORMW \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%의 자궁구는%SAMEN_DESCRIPTION(PLAYER)%(을)를 갖고 싶어하도록(듯이) 달라붙어 온다…
			ELSE
				SIF NOWEX:TARGET:V절정
					PRINTFORML 강렬한 억압이%TINKO(PLAYER)%고기원 얻을 수 있어%CALLNAME:(TARGET)%의 절정을 전해 온다
				PRINTFORML \@ NOWEX:TARGET:V절정 ? 同時に #  \@%CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 질내에 사정했다
				PRINTFORMW \@ FLAG:70 ? 時間を止められた # 眠っている \@%CALLNAME:(TARGET)%(은)는 저항도 하지 못하고 질내에%SAMEN_DESCRIPTION(PLAYER)%(을)를 쏟아지고 있다…
			ENDIF
		ENDIF
	;通常
	ELSE
		IF NOWEX:TARGET:V절정
			PRINTFORML 전신을 진동시켜 날카로운 열명을 주면서,%CALLNAME:(TARGET)%하지만 올라 채운다
			PRINTFORML %TINKO(PLAYER)%응과 질강이 억압, 와 질벽이 빨아, 정액을 재촉 하고 있다
		ENDIF
		IF TFLAG:50 == 7
			PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 질오에%TINKO(PLAYER)%(을)를 고정해
			PRINTFORMW \@ CFLAG:TARGET:900 == 7 ? 危険日の # \@자궁에 직접적으로%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			SIF EXP:TARGET:자궁구개발경험 >= 300
				PRINTFORMW %SAMEN_DESCRIPTION(PLAYER)%의 맛을 기억입 뛰어날 수 있었던 자궁은 크게 입을 열어, 탐욕에%SAMEN_DESCRIPTION(PLAYER)%(을)를 다 마시고 있다…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 질내에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 쏟았다…
			IF CFLAG:TARGET:900 == 7
				PRINTFORMW 위험일의 질육은 조금씩 준 와, 자식을 최안쪽에 보내려 하고 있다…
			ELSE
				PRINTFORMW %CALLNAME:(TARGET)%의 질육은%TINKO(PLAYER)%의 맥동에 맞추는것 같이 경련해, 사정중의%TINKO(PLAYER)%에 관련되어 붙는다…
			ENDIF
		ENDIF
		SIF TALENT:TARGET:동물귀 && TEQUIP:TARGET:체위 == 2 && NOWEX:TARGET:V절정
			PRINTFORML 암컷의 본능에 자극을 받았다%CALLNAME:(TARGET)%하%HIP((TARGET))%(을)를%CALLNAME:MASTER%의 허리에 꽉 눌러 정액을 쓸데없게 생활로 하고 있다
		IF ABL:TARGET:질내사정중독 > 3
			SIF NOWEX:TARGET:V절정
				PRINTFORML 절정과 동시에 질내에 사정되어, 군침을 흘린다%CALLNAME:(TARGET)%
			PRINTFORML 질내 사정의 감각이 견딜 수 없는 것인지,%CALLNAME:(TARGET)%(은)는 단 소리를 질러 행복 그 자체라고 하는 표정으로 사정을 받아 들이고 있다…
		ELSEIF TCVAR:TARGET:억지로
			PRINTFORML \@ NOWEX:TARGET:V절정 ? 一瞬、蕩けるような表情で絶頂の余韻に震えた #  \@%CALLNAME:(TARGET)%(은)는 허약한 비명을 흘리면서 사정을 받아 들이고 있다…
		ENDIF
		SIF NOWEX:MASTER:사정 > 1
			PRINTFORML %CALLNAME:MASTER%(은)는 녹는 것 같은 사정감에 허리를 겁내게 한이면서, 길게%SAMEN_DESCRIPTION(PLAYER)%(을)를 계속 쏟았다…
	ENDIF
;アナル
ELSEIF GETBIT(TFLAG:1,2)
	IF NOWEX:TARGET:A절정
		PRINTFORML 아픔에 걸릴 수도 있는 만큼 강하게 어널을 단단히 조여%CALLNAME:(TARGET)%하지만 올라 채운다
	ENDIF
	PRINTFORM \@ NOWEX:TARGET:A절정 ? 同時に、 #  \@%CALLNAME:PLAYER%하
	SIF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORM \@ FLAG:70 ? 時間を止めたまま # 眠っている \@
	PRINTFORML %CALLNAME:(TARGET)%의 장내에 사정했다
	PRINTFORMW %SAMEN_DESCRIPTION(PLAYER)%그리고 흘러넘치고 있는 장내는 세세하게 떨려%TINKO(PLAYER)%(을)를 힘들게 잡고 붙이고 있다…
	IF !FLAG:70 && CFLAG:MASTER:장난 != 2
		IF ABL:TARGET:항내사정중독 > 3 && !FLAG:70
			PRINTFORML 절정의 여운에 몸을 휘둘러요 등무늬, 고혈을 단단히 조여 얼굴을 느슨해지게 한다%CALLNAME:(TARGET)%
			PRINTFORML 항내 사정의 감각이 견딜 수 없는 것인지,%CALLNAME:(TARGET)%(은)는 단 소리를 질러 행복 그 자체라고 하는 표정으로 사정을 받아 들이고 있다…
		ELSEIF TCVAR:TARGET:억지로 && !FLAG:70
			PRINTFORML 억지로 올라 채우게 하고 눈초리에 눈물을 모았다%CALLNAME:(TARGET)%(은)는, 허약한 비명을 흘리면서 사정을 받아 들이고 있다…
		ENDIF
	ENDIF
;手淫
ELSEIF GETBIT(TFLAG:1,3)
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %CALLNAME:MASTER%하%TINKO(PLAYER)%(을)를 훑어 내게 하고 있었다%CALLNAME:(TARGET)%의 손에 귀두를 꽉 눌러 사정했다…
	ELSE
		IF CFLAG:우후후 == 2
			PRINTFORML 사정이 시작되면(자)%CALLNAME:(TARGET)%(은)는 한층 격렬하고%TINKO(PLAYER)%(을)를 훑어 내 세우고
			PRINTFORML \@ NOWEX:PLAYER:사정 > 1 ? 大量に噴き上がる # 吐き出される \@%SAMEN_DESCRIPTION(PLAYER)%(을)를 만족스럽게 바라보면서
			PRINTFORMW %CALLNAME:MASTER%의%TINKO(PLAYER)%의 맥동이 멈출 때까지 애무를 계속했다…
		ELSE
			IF ABL:TARGET:정액중독 >= 5 && ABL:TARGET:기교 > 3
				PRINTFORML %CALLNAME:(TARGET)%(은)는 황홀의 표정으로 혀를 쑥 내밀면서%CALLNAME:PLAYER%의%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 받고 있다…
			ELSEIF NOWEX:PLAYER:사정 == 1
				PRINTFORMW %CALLNAME:(TARGET)%에%SAMEN_DESCRIPTION(PLAYER)%하지만 닥쳐 갔다…
			ELSE
				PRINTFORMW %CALLNAME:(TARGET)%의 얼굴과 손이 대량의%SAMEN_DESCRIPTION(PLAYER)%투성이가 되어 갔다…
			ENDIF
		ENDIF
	ENDIF
;フェラチオ
ELSEIF GETBIT(TFLAG:1,4)
	IF NOWEX:TARGET:M절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORML %TINKO(PLAYER)%에 구강을 비비어 세울 수 있어,%CALLNAME:(TARGET)%하지만 입술을 진동시키고 올라 채우고 있다
		ELSE
			PRINTFORML 입안을 묻는다%TINKO(PLAYER)%에 넑을 잃은 표정을 띄운다%CALLNAME:(TARGET)%
			PRINTFORML 한 박자 두어, 입술을 움츠리면서 어깨를 진동시켜%CALLNAME:(TARGET)%하지만 올라 채웠다
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW %CALLNAME:MASTER%하%TINKO(PLAYER)%(을)를%CALLNAME:(TARGET)%의 후오까지 찔러 넣어 사정했다…
			CASE 1
				PRINTFORMW %CALLNAME:MASTER%하%CALLNAME:(TARGET)%에 귀두를 입에 물게 해 혀 위에%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 토해냈다…
		ENDSELECT
	ELSE
		IF CFLAG:우후후 == 2
			PRINTFORML %CALLNAME:MASTER%하%CALLNAME:(TARGET)%의 입안에 사정했다…
			PRINTFORMW %CALLNAME:(TARGET)%(은)는 입안에서%SAMEN_DESCRIPTION(PLAYER)%(을)를 받으면서 혀로 귀두를 애무해, 새로운 사정을 재촉하고 있다…
		ELSEIF SELECTCOM == 140
			PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 목의 안쪽까지%TINKO(PLAYER)%(을)를 삽입한 채로 사정해
			PRINTFORMW %SAMEN_DESCRIPTION(PLAYER,1)%(을)를 식도에 보냈다…
		ELSE
			PRINTFORMW %CALLNAME:PLAYER%하%CALLNAME:(TARGET)%의 입안에 사정했다
			;精飲
			IF EXP_UP(15,TARGET) == NOWEX:PLAYER:사정 * 3
				IF NOWEX:PLAYER:사정 == 1
					PRINTFORML %CALLNAME:(TARGET)%(은)는 입술로 귀두를 훑어 내 모든%SAMEN_DESCRIPTION(PLAYER)%(을)를 입안에 받아 들이고 나서
					PRINTFORMW 입안의%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 군침이 돌아 삼켰다…
				ELSE
					PRINTFORMW %CALLNAME:(TARGET)%(은)는 귀두를 질입안에 발해지는 대량의%SAMEN_DESCRIPTION(PLAYER)%(을)를 삼켜 갔다…
				ENDIF
				;RETURN 1
			ELSE
				IF TCVAR:TARGET:정음경험 == 0
					PRINTFORML %CALLNAME:(TARGET)%(은)는 놀라%TINKO(PLAYER)%(으)로부터 입을 떼어 놓아 버렸다
					PRINTFORMW 얼굴을%SAMEN_DESCRIPTION(PLAYER)%그리고 더럽히면서 처음의 맛에 조금 당황하고 있는 것 같다…
				ELSEIF TCVAR:TARGET:정음경험 < 20
					PRINTFORML 사정이 끝나면(자)%CALLNAME:(TARGET)%(은)는 입속의%SAMEN_DESCRIPTION(PLAYER)%(을)를 토해내 버렸다
					PRINTFORMW 아직%SAMEN_DESCRIPTION(PLAYER)%의 맛에 익숙하지 않는 것 같다…
				ELSE
					IF NOWEX:PLAYER:사정 == 1
						PRINTFORML %CALLNAME:(TARGET)%(은)는 입술로 귀두를 훑어 내 모든%SAMEN_DESCRIPTION(PLAYER)%(을)를 입안에 받아 들이고 나서
						PRINTFORMW 넓힌 손 위에%SAMEN_DESCRIPTION(PLAYER)%(을)를 토출%CALLNAME:PLAYER%에 과시해 왔다…
					ELSE
						PRINTFORML %CALLNAME:(TARGET)%(은)는 혀를 쑥 내밀면서%SAMEN_DESCRIPTION(PLAYER,1)%하지만 불기 시작하는 요도구를 자극해,
						PRINTFORMW 열린 입으로부터는%SAMEN_DESCRIPTION(PLAYER)%하지만 다라 다라와 넘쳐 흐름으로라고 있다…
					ENDIF
				ENDIF
				;RETURN 2
			ENDIF
		ENDIF
	ENDIF
;パイズリ
ELSEIF GETBIT(TFLAG:1,5)
	PRINTFORM %CALLNAME:MASTER%하
	SIF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORM \@ FLAG:70 ? 時間の止まった # 眠っている \@
	PRINTFORM %CALLNAME:(TARGET)%의
	IF TALENT:가슴사이즈 > 0
		PRINTFORMW ,%OPPAI_DESCRIPTION((TARGET))%에%TINKO(PLAYER)%(을)를 사이에 둔 채로 사정했다…
	ELSEIF TALENT:가슴사이즈 < 0
		PRINTFORMW ,%OPPAI_DESCRIPTION((TARGET))%에%TINKO(PLAYER)%(을)를 칠하면서 사정했다…
	ELSE
		PRINTFORMW %OPPAI_DESCRIPTION((TARGET))%에%TINKO(PLAYER)%(을)를 사이에 둔 채로 사정했다…
	ENDIF
	IF NOWEX:PLAYER:사정 == 1
		IF TALENT:가슴사이즈 > 0
			PRINTFORMW %CALLNAME:(TARGET)%의%OPPAI_DESCRIPTION((TARGET))%의 골짜기에%SAMEN_DESCRIPTION(PLAYER,1)%하지만 모여 간다…
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)%의 가슴 팍으로부터 얼굴에까지%SAMEN_DESCRIPTION(PLAYER,1)%하지만 흩날렸다…
		ENDIF
	ELSE
		PRINTFORMW 흩날렸다%SAMEN_DESCRIPTION(PLAYER)%하지만%CALLNAME:(TARGET)%의 얼굴로부터 가슴 팍까지를 가리고 있다…
	ENDIF
	IF NOWEX:TARGET:B절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORMW 다 날카로워진 유두와 떨린다%OPPAI_DESCRIPTION((TARGET))%하지만,%CALLNAME:(TARGET)%의 절정을 이슬 악어 하고 있다
		ELSE
			PRINTFORML 꽉%OPPAI_DESCRIPTION((TARGET))%(을)를%TINKO(PLAYER)%에 꽉 눌러%CALLNAME:(TARGET)%(은)는 절정에 이르고 있다
		ENDIF
	ENDIF
;疑似
ELSEIF GETBIT(TFLAG:1,25)
	PRINTFORM %CALLNAME:PLAYER%하
	IF FLAG:70
		PRINTFORM 시간을 멈추었다
	ELSEIF CFLAG:MASTER:장난 == 2
		PRINT 자고 있다
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 질구에 문지르고 있던 페니스를 성기의 바로 위에 꽉 눌러 사정했다…
	PRINTFORMW %CALLNAME:TARGET%의 아랫배가%SAMEN_DESCRIPTION(PLAYER)%그리고 더럽혀져 간다…
;素股
ELSEIF GETBIT(TFLAG:1,6)
	IF !GETBIT(TALENT:성별,1) && NOWEX:TARGET:C절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORML 넌더리나고 결림이 된 클리토리스를%TINKO(PLAYER)%그리고 비비어져%CALLNAME:(TARGET)%하지만 올라 채웠다
		ELSE
			PRINTFORML 구리를 비비어지는 감각에,%CALLNAME:(TARGET)%(은)는 몸을 진동시키고 올라 채우고 있다
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %CALLNAME:PLAYER%하\@ FLAG:70 ? 時間を止めた # 眠っている \@%CALLNAME:(TARGET)%의 성기에%TINKO(PLAYER)%(을)를 문지르면서 사정했다…
	ELSE
		PRINTFORMW %CALLNAME:PLAYER%(은)는 밀착한 성기의 사이에 사정했다…
	ENDIF
;足コキ
ELSEIF GETBIT(TFLAG:1,7)
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %CALLNAME:PLAYER%하\@ FLAG:70 ? 時間を止めた # 眠っている \@%CALLNAME:(TARGET)%의 다리에%TINKO(PLAYER)%(을)를 문지르면서 사정했다…
	ELSE
		PRINTFORM %CALLNAME:(TARGET)%의
		SIF TALENT:TARGET:새드
			PRINTFORM 서늘한 시선을 받으면서, 그
		PRINTFORMW 다리에\@ NOWEX:MASTER:사정 == 2 ? 大量の # 熱い \@%SAMEN_DESCRIPTION(PLAYER)%(을)를 발했다…
	ENDIF
;泡踊り
ELSEIF GETBIT(TFLAG:1,8)
	IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
		PRINTFORML %CALLNAME:(TARGET)%(와)과%CALLNAME:PLAYER%(은)는 서로 꼭 껴안은 채로 동시에 정액을 발했다…
		PRINTFORMW 두 명은 벌벌정령을 발하는 음경을 비비어 맞추도록(듯이) 서로의 몸을 서로 요구했다…
	ELSEIF NOWEX:TARGET:사정
		PRINTFORMW %CALLNAME:(TARGET)%하%TINKO(PLAYER)%(으)로부터%SAMEN_DESCRIPTION(PLAYER)%(을)를 발하면서%CALLNAME:PLAYER%에 몸을 바짝 다가오고 키라고 있다…
	ELSE
		PRINTFORMW %CALLNAME:(TARGET)%하%CALLNAME:PLAYER%하지만 발했다%SAMEN_DESCRIPTION(PLAYER,1)%(을)를 문질러 바르도록(듯이) 몸을 밀착시키고 있다…
	ENDIF
;アナル奉仕
ELSEIF GETBIT(TFLAG:1,9)
	PRINTFORMW 머리를 아래로 해 몸을 휨 당했다%CALLNAME:PLAYER%의 얼굴은, 토해 출 당한 대량의%SAMEN_DESCRIPTION(PLAYER)%투성이가 되어 갔다…
;Ａ責め
ELSEIF GETBIT(TFLAG:1,12)
	PRINTFORM 허리의 안쪽이 저리는 것 같은 쾌락과 함께,%CALLNAME:PLAYER%의 페니스로부터
	IF NOWEX:MASTER:사정 >= 2
		PRINTFORML 대량의%SAMEN_DESCRIPTION(PLAYER)%하지만 드크드크와 빠져 나오고 했다…
		PRINTFORMW %CALLNAME:PLAYER%(은)는 끝없이%SAMEN_DESCRIPTION(PLAYER)%하지만 빠져 나오고 해 나가는 쾌감에 몇번이나 허리를 진동시키면서 번민…
	ELSE
		PRINTFORMW %SAMEN_DESCRIPTION(PLAYER)%하지만 다랑어 다랑어와 빠져 나오고 했다…
	ENDIF
;Ｖ責め
ELSEIF GETBIT(TFLAG:1,13)
	IF NOWEX:MASTER:사정 >= 2
		PRINTFORML 허리의 안쪽이 저리는 것 같은 쾌락과 함께,%CALLNAME:PLAYER%의 페니스로부터 대량의%SAMEN_DESCRIPTION(PLAYER)%하지만 드크드크와 빠져 나오고 했다…
		PRINTFORMW 암컷의 쾌락을 맛보면서의 대량 사정으로%CALLNAME:PLAYER%(은)는 머리가 새하얗게 되는 것을 느꼈다…
	ELSE
		PRINTFORMW 허리의 안쪽이 저리는 것 같은 쾌락과 함께,%CALLNAME:PLAYER%의 페니스로부터%SAMEN_DESCRIPTION(PLAYER)%하지만 다랑어 다랑어와 빠져 나오고 했다…
	ENDIF
ENDIF

IF CFLAG:TARGET:우후후 == 2
	SIF !(NOWEX:TARGET:C절정 + NOWEX:TARGET:V절정 + NOWEX:TARGET:A절정 + NOWEX:TARGET:B절정 + NOWEX:TARGET:M절정)
		PRINTFORML %CALLNAME:(TARGET)%(은)는 절정에 이완 나누었다%CALLNAME:MASTER%의 표정을 바라봐 능글능글 하고 있다…
	IF GROUPMATCH(TCVAR:PLAYER:사정한개소플래그,1,2)
		CALL SHIBORITORI(MASTER,(TARGET))
	ELSEIF GROUPMATCH(TCVAR:TARGET:사정한개소플래그,1,2)
		CALL SHIBORITORI((TARGET),MASTER)
	ENDIF
ENDIF

		

SIF FLAG:대사색깔
	TRYCALLFORM M_KOJO_COLOR_K{NO:TARGET}

@PrintMilkAndOmorashi(ARG, OMORASHI)
#DIM OMORASHI

;絶頂と噴乳とおもらし

SIF ARG >= 3
	PRINTS CALLNAME:TARGET
IF ARG >= 5 && TEQUIP:50 > 0
	PRINTL 의 육 벽은 끈적거리는 액체를 방울져 떨어지게 해 허덕이도록(듯이) 개폐를 반복하고 있다…
ELSEIF ARG >= 5
	PRINTL (은)는 단속적으로 절정을 반복하면서, 축탈진하고 있다…
ELSEIF ARG >= 3
	PRINTFORML  하\@CFLAG:MASTER:장난 == 2 ?거친 숨소리를 내쉬며 #거친 숨을 쉬며 \@절정의 여운에 잠겨 있다…
ENDIF

IF (ARG >= 7 && TCVAR:이뇨제 && TALENT:오줌싸개버릇) || (ARG >= 9 && NOWEX:분유)
	PRINTFORM %CALLNAME:TARGET%(은)는 가크가크와 신체를 경련시켜,
ELSEIF (ARG >= 7 && OMORASHI) || (ARG >= 7 && NOWEX:분유)
	PRINTFORM 경련한다%CALLNAME:TARGET%하
ELSEIF (ARG >= 5 && OMORASHI) || (ARG >= 5 && NOWEX:분유)
	PRINTFORM 축으로 했다%CALLNAME:TARGET%하
ELSEIF (ARG >= 3 && OMORASHI) || (ARG >= 3 && NOWEX:분유)
	PRINTFORM 떨린다%CALLNAME:TARGET%하
ELSEIF (ARG >= 1 && OMORASHI) || NOWEX:분유
	PRINTFORM %CALLNAME:TARGET%하
ENDIF
IF NOWEX:분유
	IF ARG >= 9
		LOCALS = 飛び散るほどに勢いよく母乳を噴出し
	ELSEIF ARG >= 7
		LOCALS = びゅーびゅーと母乳を噴出し
	ELSEIF ARG >= 5
		LOCALS = 垂れ流すように母乳を噴出し
	ELSEIF ARG >= 3
		LOCALS = 母乳を噴出させ
	ELSE
		LOCALS = 母乳を垂らし
	ENDIF
	PRINTFORM %NIPPLE_DESCRIPTION(TARGET)%(으)로부터%LOCALS%
	IF OMORASHI
		PRINTFORM (이)면서,
	ELSE
		PRINTFORML …
	ENDIF
ENDIF
IF ARG >= 7 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 멈춤곳 없게 뇨를 흘려 보내고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 뇨가 흠뻑스며들어 간다…
ELSEIF ARG >= 7 && TCVAR:이뇨제
	PRINTFORML 요도구로부터 분수와 같이 뇨를 분출하게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 뇨가 흠뻑스며들어 간다…
ELSEIF ARG >= 7 && TALENT:오줌싸개버릇
	PRINTFORML 요도구로부터 기세 좋게 수류를 내뿜게 해 깨끗한 호를 그리게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 뇨가 흠뻑스며들어 간다…
ELSEIF ARG >= 5 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구로부터 분수와 같이 뇨를 분출하게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 되어 있다…
ELSEIF ARG >= 5 && TCVAR:이뇨제
	PRINTFORML 요도구로부터 기세 좋게 수류를 내뿜게 해 깨끗한 호를 그리게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 되어 있다…
ELSEIF ARG >= 5 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 수류를 방울져 떨어지게 해 웅덩이를 만들었다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 되어 있다…
ELSEIF ARG >= 3 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구로부터 기세 좋게 수류를 내뿜게 해 호를 그리게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 되어 있다…
ELSEIF ARG >= 3 && TCVAR:이뇨제
	PRINTFORML 요도구에서 수류를 방울져 떨어지게 해 웅덩이를 만들었다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 되어 있다…
ELSEIF ARG >= 3 && TALENT:오줌싸개버릇
	PRINTFORML 음순으로부터 뇨를 졸졸빠져 나오게 하고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 되어 있다…
ELSEIF ARG >= 1 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 수류를 방울져 떨어지게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 작은 얼룩이 되어 있다…
ELSEIF ARG >= 1 && TCVAR:이뇨제
	PRINTFORML 음순으로부터 뇨를 졸졸빠져 나오게 하고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 작은 얼룩이 되어 있다…
ENDIF
IF TEQUIP:유두빨기 && NOWEX:분유
	SELECTCASE ARG
		CASE IS >= 9
			LOCALS = 口の端から垂れ流しながら、ごくごくと喉を鳴らして母乳を飲んでいる…
		CASE IS >= 7
			LOCALS = 口の端から零れるほどの母乳を飲んでいる…
		CASE IS >= 5
			LOCALS = 次々と溢れ出てくる母乳を飲んでいる…
		CASE IS >= 3
			LOCALS = 母乳を飲んでいる…
		CASEELSE
			LOCALS = 滲み出る母乳を舌で舐めとっている…
	ENDSELECT
	PRINTFORML %CALLNAME:MASTER%하%CALLNAME:TARGET%의%NIPPLE_DESCRIPTION(TARGET)%(을)를 빨아 올려%LOCALS%
ENDIF
